.yandexMap {
  background-color: var(--colorAthensGray);
  padding: 80px 0;

  &__title {
    margin-bottom: 50px;
    font-family: "Proxima Nova", sans-serif;
    font-style: normal;
    font-weight: 700;
    font-size: 40px;
    line-height: 50px;
    color: var(--colorShark);
  }

  &__inner {
    width: 100%;
    height: 340px;
  }
}
